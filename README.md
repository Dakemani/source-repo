# source-repo
# Проект: Скрипт синхронизации локальных репозиториев

## Описание проекта
Данный проект представляет собой **готовое к использованию** решение для автоматической синхронизации двух локальных Git-репозиториев. Скрипт разработан для операционной системы Windows (PowerShell) и выполняет полную синхронизацию содержимого репозиториев с сохранением структуры папок и файлов.

## Возможности скрипта
- 🔄 **Автоматическое обновление** исходного репозитория через `git pull`
- 📋 **Полное копирование** всех файлов и папок (кроме служебной папки `.git`)
- ✅ **Проверка наличия Git** в системе
- 📁 **Автоматическое создание** целевой папки, если она отсутствует
- 🛡️ **Защита от ошибок** - проверка существования путей перед копированием
- 📊 **Детальный вывод** каждого шага с цветовой индикацией
- ⏸️ **Пауза в конце** - окно не закроется до нажатия клавиши

## Как это работает
Скрипт выполняет последовательно следующие действия:

1. **Проверка Git** - убеждается, что Git установлен в системе
2. **Проверка путей** - проверяет существование исходного репозитория
3. **Создание целевой папки** - если её нет, она будет создана автоматически
4. **Git pull** - обновляет исходный репозиторий из удаленного источника
5. **Копирование** - переносит все файлы и папки (кроме `.git`) в целевой репозиторий
6. **Отчет** - показывает статистику скопированных файлов и папок

## Требования к системе
- **Операционная система**: Windows 7/8/10/11
- **PowerShell**: версия 5.1 или выше (встроен в Windows)
- **Git**: любая современная версия ([скачать](https://git-scm.com/download/win))

## Полная инструкция по установке и запуску

### Шаг 1: Установка Git (если ещё не установлен)
1. Перейдите на [официальный сайт Git](https://git-scm.com/download/win)
2. Скачайте установщик для вашей системы (64-bit или 32-bit)
3. Запустите установку, **обязательно выберите опцию**:
   - "Git from the command line and also from 3rd-party software" (это добавит Git в PATH)
4. После установки перезапустите PowerShell

### Шаг 2: Создание скрипта
1. Нажмите `Win + R`, введите `notepad` и нажмите Enter
2. Скопируйте код скрипта из файла `sync_repos.ps1` в Блокнот
3. Нажмите `Ctrl + S` (Сохранить)
4. Выберите папку для сохранения (например, `C:\scripts`)
5. В поле "Имя файла" введите: **`sync_repos.ps1`**
6. В поле "Тип файла" выберите **"Все файлы (*.*)"**
7. Нажмите "Сохранить"

### Шаг 3: Настройка путей к репозиториям
Откройте сохраненный файл `sync_repos.ps1` в Блокноте и найдите в начале файла строки:

```powershell
$SOURCE_REPO = "C:\Projects\source-repo"     # Отсюда берем файлы
$TARGET_REPO = "C:\Projects\target-repo"     # Сюда копируем